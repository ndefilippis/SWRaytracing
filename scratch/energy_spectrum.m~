test = zeros(1,frame);
for i=1:frame
    qk = g2k(q_save(:,:,i));
    psik = -qk./(K_d2 + K2);
    uk = -1i*ky_.*psik;
    vk =  1i*kx_.*psik;
    %KE = sum(sum(uk.*conj(uk) + vk.*conj(vk)));
    KEk = K2.*abs(psik).^2;
    KE = zeros(1,kmax);
    KE = s
end
plot(test);
